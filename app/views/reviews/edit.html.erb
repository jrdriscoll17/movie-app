<%= @review.movie.title %>
<%= form_for([@review.user, @review]) do |f| %>
  <%= f.label :rating %>
  <%= f.number_field :rating %><br>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit %>
<% end %>
